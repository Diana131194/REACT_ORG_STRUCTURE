import { ViewEmployeeActionsConstants } from './constants'
import initialState from '../../initialState'

const ViewEmployeeReducer = (state = initialState.employee, action) => {

    switch (action.type) {
        default:
            return state
    }
}

export default ViewEmployeeReducer
